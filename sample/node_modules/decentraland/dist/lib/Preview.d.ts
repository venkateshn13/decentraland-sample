/// <reference types="node" />
import { EventEmitter } from 'events';
import { Decentraland } from './Decentraland';
/**
 * Events emitted by this class:
 *
 * preview:ready - The server is up and running
 */
export declare class Preview extends EventEmitter {
    dcl: Decentraland;
    private app;
    private server;
    private wss;
    private watch;
    constructor(dcl: Decentraland, watch: boolean);
    startServer(port: number): Promise<unknown>;
}
//# sourceMappingURL=Preview.d.ts.map