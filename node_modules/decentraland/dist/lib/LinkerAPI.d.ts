/// <reference types="node" />
import { EventEmitter } from 'events';
import { ChainId } from '@dcl/schemas';
import { Project } from './Project';
export declare type LinkerResponse = {
    address: string;
    signature: string;
    chainId?: ChainId;
};
/**
 * Events emitted by this class:
 *
 * link:ready   - The server is up and running
 * link:success - Signatire success
 * link:error   - The transaction failed and the server was closed
 */
export declare class LinkerAPI extends EventEmitter {
    private project;
    private app;
    constructor(project: Project);
    link(port: number, isHttps: boolean, rootCID: string): Promise<unknown>;
    private setRoutes;
}
//# sourceMappingURL=LinkerAPI.d.ts.map